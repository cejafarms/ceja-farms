# ---------------------------------------------------------------------------
# Ceja Farms — what stays out of the repo
#
# The site itself (HTML/CSS/JS, images/, video/) IS tracked, because those are
# the files that get deployed. What's excluded below is source material and
# machine cruft.
# ---------------------------------------------------------------------------

# Raw camera/drone masters. The web-ready encodes in /video are committed; these
# ~215 MB originals are the inputs they were cut from. Keep them backed up
# somewhere else. To track one anyway: git add -f "pulling vinyards.MP4"
/Moving debree.MP4
/Moving equiptment.MP4
/pulling vinyards.MP4
/1B268E17-57C9-4473-AD55-2C1DEB57E605.MOV
/4BEF4FF1-90B5-4CB0-86B0-5728DD11260C.MOV
/D2986A9B-B1FB-4F7F-AFAB-A975406999DA.MOV
# Drone flights added 10 Sep 2026 (vineyard flyover + sunrise cherry road).
/VID_20260906_11453300.MP4
/VID_20260909_07112300.MP4

# Phone photos emailed in on 8 Sep 2026. The crops cut from them live in
# /images under their slot names; these full-size originals stay out.
/IMG_3025.jpeg
/IMG_3028.jpeg

# Page-hero images supplied 10 Sep 2026. Converted to web JPGs in /images
# under their slot names (about-hero.jpg etc.); the ~15 MB PNG originals stay out.
/About Hero.png
/Contact Us Hero.png
/Our Projects Hero.png
/Removal Hero.png
/Rentals Hero.png
/Vinyard Services Hero.png

# iCloud drops. The three archives here turned out to be byte-identical copies
# of the drone masters above (verified by MD5), not photos - so ignore both the
# archives and anything unzipped from them, however it lands.
images/*.zip
images/iCloud Photos from */
**/iCloud Photos from Maria Elwin/
images/VID_*.MP4
images/*.MP4
images/*.MOV

# OS / file-manager droppings
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store
$RECYCLE.BIN/

# Editors
.vscode/
.idea/
*.swp
*~

# Tooling, if it ever lands here
node_modules/
npm-debug.log*
.env
.env.local
